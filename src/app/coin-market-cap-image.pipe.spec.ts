import { CoinMarketCapImagePipe } from './coin-market-cap-image.pipe';

describe('CoinMarketCapImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CoinMarketCapImagePipe();
    expect(pipe).toBeTruthy();
  });
});
